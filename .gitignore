node_modules/
dist/
*.log
.DS_Store
Thumbs.db
.vscode/
# Локальные настройки/конфиги Claude Code — не относятся к самому расширению
.claude/

# Генерируется `npm run setup` — не коммитим (см. scripts/prepare-assets.mjs)
public/wasm/
public/ort/
public/models/

# Готовые архивы для GitHub Release — не коммитим в git, только как Release asset
*.zip
